# 과천 내집마련 대시보드 — 제작 계획서

> 작성일 2026-07-17 · 기존 `gwacheon-planner-v2.jsx` + `과천_신혼부부_재무설계_총정리.md` 기반 확장

---

## 1. 목표

기존 재무설계 플래너(planner-v2)의 모든 기능을 유지하면서, **청약 정보 + 네이버 부동산 매물 + 네이버 지도**를 조건 검색으로 조회할 수 있는 통합 대시보드 사이트를 만든다.

추가로, 앞으로 이 프로젝트를 이어서 작업할 두 개의 서브 에이전트를 정의한다.

- **realty-finance-agent** — 재무설계 · 청약 전략 · 알림 담당
- **html-site-builder** — HTML 대시보드/사이트 제작 담당

---

## 2. 산출물

| 경로 | 설명 |
|---|---|
| `PLAN.md` | 본 계획서 |
| `.claude/agents/realty-finance-agent.md` | 재무·청약·알림 에이전트 정의 |
| `.claude/agents/html-site-builder.md` | HTML 사이트 제작 에이전트 정의 |
| `dashboard/index.html` | 대시보드 본체 (단일 파일, 바로 실행 가능) |
| `dashboard/server.js` | (선택) 실데이터 프록시 + 정적 서버 (Node) |
| `dashboard/data/samples.js` | 청약·매물 샘플 데이터 (오프라인 폴백) |
| `dashboard/README.md` | 실행법 · API 키 발급법 · 프록시 사용법 |

---

## 3. 기능 범위

### 3.1 기존 planner-v2 기능 (그대로 이식)
- 진단(세후소득·DSR·LTV·가격구간 하드캡·자기자본 갭·달성기간)
- 전략·혜택(청약/매매/전세 비교, 제도 활용 가능 여부)
- 자산관리(ISA·연금저축·IRP, 배우자 증여세 계산기)
- 대출계산기(3단 필터 + 원리금/원금균등 이자 시뮬레이션)
- 타임라인 / 체크리스트
- 저장 방식: `window.storage` → **`localStorage`** 로 교체(스탠드얼론 실행 위해)

### 3.2 신규 — 청약 정보 탭
- 조건 필터: **지역 · 공급유형(신혼/신생아/생애최초/일반) · 평형대 · 분양가 범위 · 접수마감 이전만**
- 목록 + 상세(접수일/발표일/입주예정/분양가/특별공급 세대수)
- 데이터 소스: 공공데이터포털 **「한국부동산원_청약홈 APT 분양정보 조회」** REST API
  - CORS 미지원 → `server.js` 프록시 경유. 프록시 없으면 샘플데이터 사용
- 지도 연동: 각 단지 위치를 지도 탭 마커로 표시

### 3.3 신규 — 부동산 매물 탭
- 조건 필터: **지역 · 거래유형(매매/전세/월세) · 평형 · 가격 범위**
- 목록(단지·평형·가격·층·준공·거리) + 지도 마커
- 데이터 소스: 네이버 부동산 (⚠️ **공식 API 없음**)
  - `server.js` 프록시가 `new.land.naver.com` 내부 API를 대리 호출(비공식·ToS 유의)
  - 프록시 없으면 과천 주요 단지 샘플데이터로 동작

### 3.4 신규 — 네이버 지도
- 네이버 클라우드 플랫폼 **Maps JS SDK**(브라우저 키, `ncpKeyId`)
- 청약 단지 + 매물을 마커로. 마커 클릭 시 정보창
- 키 미입력 시: 설정(⚙️)에서 키 붙여넣기 → `localStorage` 저장 후 지도 활성화(파일 수정 불필요)

---

## 4. 아키텍처 & 기술 결정

- **프론트**: 단일 `index.html` + React(UMD CDN) + Babel Standalone + Tailwind Play CDN — 기존 JSX 스타일/디자인 토큰 그대로 승계(웜톤 세리프: `#FAF7F0`/`#9C7A22`/`#1F5D46`).
- **왜 단일 파일인가**: 사용자가 더블클릭만으로 열어볼 수 있고, 실데이터가 필요할 때만 `node server.js`로 승격.
- **데이터 레이어 추상화**: `dataSource = live(proxy) ?? sample`. 프록시 미가동/키 없음이면 자동으로 샘플로 폴백하고 배지로 상태 표시.
- **CORS 현실**:
  - 청약홈/공공데이터 API·네이버 부동산 내부 API는 브라우저 직접호출 불가 → 프록시 필수.
  - 네이버 지도 SDK는 브라우저 직접 로드 가능(도메인 등록된 키 필요).

### 필요한 키 (사용자 발급)
| 용도 | 발급처 | 프론트/서버 |
|---|---|---|
| 네이버 지도 | NCP → Maps → Application(`ncpKeyId`) | 프론트(설정에 입력) |
| 청약 정보 | data.go.kr 「청약홈 분양정보」 활용신청(serviceKey) | 서버(.env) |
| 네이버 매물 | 없음(비공식 프록시) | 서버 |

---

## 5. 진행 순서
1. ✅ 프로젝트 파악
2. 계획서(본 문서)
3. 에이전트 2종 정의
4. 대시보드 빌드(기존 이식 → 신규 탭 → 지도 → 프록시 → README)
5. 로컬 실행 확인 및 인수인계

---

## 6. 한계 · 유의사항
- 네이버 부동산 크롤링은 비공식이며 서비스 약관/차단 정책에 영향받음. 개인 참고용으로만 사용 권장.
- 모든 금액·제도 수치는 2026 상반기 공개자료 기반 추정치. 실행 전 전문가 확인 필요.
- 청약홈 공식 API 필드는 공고 유형별로 상이 → 어댑터에서 정규화.
